{
  "name": "base-cyber-miner",
  "version": "1.0.0",
  "description": "Base Sepolia Web3 Cyber Clicker & Coin Flip Game",
  "main": "index.html",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arc-testnet",
    "deploy:arc": "hardhat run scripts/deploy.js --network arc-testnet",
    "test": "hardhat test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  }
}
